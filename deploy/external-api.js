var EXTERNAL_API_A = function(message,location){

	jQuery(location).after('Method A = '+message+'</BR>');
	
}

var EXTERNAL_API_B = function(message,location){
	
	jQuery(location).after('Method B = '+message+'</BR>');
	
}

